title: "Target E-Commerce Revenue Analysis"
description: "A project analyzing e-commerce revenue."
theme: "minima" # Optional, only if you're using a theme like minima
markdown: "kramdown"
permalink: "pretty" # Optional: pretty URLs for pages

# Baseurl and URL configuration
url: "https://melodicmetrics.github.io"
baseurl: "/Target-ECommerce-Revenue-Analysis" 

# Plugins
plugins:
  - "jekyll-feed" # Optional: Adds RSS feed functionality (useful if hosting)
  - "jekyll-sitemap" # Optional: Automatically generates a sitemap
